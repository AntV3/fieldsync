# FieldSync — Staging Environment
# Used by: npm run build:staging / npm run dev:staging
# Points to a separate Supabase project for safe testing
#
# To set up staging:
#   1. Create a new Supabase project (e.g., "fieldsync-staging")
#   2. Apply all migrations from supabase/migrations/ to the staging DB
#   3. Copy the project URL and anon key below
#   4. In Vercel, set these as env vars for Preview deployments

VITE_SUPABASE_URL=https://your-staging-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-staging-anon-key
